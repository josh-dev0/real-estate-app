overwrite: true
schema: "http://localhost:5000"
documents: "graphql/**/*.graphql"
generates:
  graphql/types.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
